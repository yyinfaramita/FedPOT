# FedPOT
The code is in the folder named "FedPOT" and mainly contains the files of two attack methods (over-claim and under-train), an incentive mechanism (Refiner), and an aggregation method (FedAvg).
